<div class="bg-gray-100" data-controller="flash">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-3">
    <%= render "/@shared/flash/success" if flash[:success].present? %>
    <%= render "/@shared/flash/error" if flash[:error].present? %>
  </div>
</div>
